{
    "name": "mediagone/vue-in-twig-bundle",
    "description": "Integrates Vue.js 3 into Symfony/Twig without any Node.js toolchain — extends it with Twig's server-side power to compose your components, generate safe Symfony URLs with dynamic parameters, and inject PHP constants or initial data directly into them.",
    "type": "symfony-bundle",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "symfony/framework-bundle": "^6.1|^7.0",
        "twig/twig": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "Mediagone\\VueInTwigBundle\\": "src/"
        }
    },
    "scripts": {
        "demo": "php -S localhost:8000 demo/index.php"
    }
}
